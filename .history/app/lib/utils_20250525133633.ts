
export function histogramGrid(quality: string, map: Function, iterations: number) {
    if (quality = 'medium') {
        const width = 1920;
        const height =  1080;

    } else if(quality = 'high') {
        const width = 3840;
        const height =  2160;

    }
}
